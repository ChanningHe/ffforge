services:
  ffmpeg-web:
    build:
      context: .
      dockerfile: Dockerfile.nvidia
    container_name: ffmpeg-web-nvidia
    ports:
      - "38110:8080"
    volumes:
      # Mount your video files directory
      - /Volumes/Codes/video:/data
      # Mount output directory
      - ./output:/output
      # Mount config directory
      - ./config:/app/config
      # Mount database directory for persistence
      - ./database:/app/database
    environment:
      - GIN_MODE=release
      - PORT=8080
      - DATA_PATH=/data
      - OUTPUT_PATH=/output
      - CONFIG_PATH=/app/config
      - DATABASE_PATH=/app/database/ffmpeg-web.db
      - MAX_CONCURRENT_TASKS=4
      - ENABLE_GPU=true
    # NVIDIA GPU configuration
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, video, compute, utility]
    restart: unless-stopped
    networks:
      - ffmpeg-network

networks:
  ffmpeg-network:
    driver: bridge

